import { makeStyles } from "@mui/styles";

export const useLayoutStyles = makeStyles(() => ({
  container: {
    maxWidth: "480px",
    width: "100%",
    position: "relative",
    margin: "0 auto",
    // padding: "0px 16px 80px",
    height: "100vh",
  },
}));
